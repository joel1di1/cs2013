<%= form_for(@enonce) do |f| %>
  <% if @enonce.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@enonce.errors.count, "error") %> prohibited this enonce from being saved:</h2>

      <ul>
      <% @enonce.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
